export interface Alumno {
      idalumnos: number;
      nombre_completo: string;
      correo: string;
      telefono: string;
      no_control: string;
      foto: string;
      contrasena: string;
      fecha_registro: string; // Formato ISO
}
